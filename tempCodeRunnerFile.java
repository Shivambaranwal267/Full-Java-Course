// int max = Integer.MIN_VALUE;
    // for (int i = 0; i < n; i++) {
    //   for (int j = 0; j < n; j++) {
    //     if (Math.abs(arr[i] - arr[j]) + i - j > max) {
    //       max = Math.abs(arr[i] - arr[j]) + i - j;
    //     }
    //   }
    // }